import { bsTheme } from '../_vars.css'
import { body } from '../../bootstrap/body.css'
import { containerFluid } from '../../bootstrap/container.css'

export const sketchyGlobalRuntimeClasses = {
	bsTheme,
	body,
	containerFluid,
} as const
