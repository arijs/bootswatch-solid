import { style } from '@vanilla-extract/css'

// Contract classes: zero-style identifiers shared across all themes.

// ── Spinner base types ────────────────────────────────────────────────────────

export const spinnerBorder = style({})
export const spinnerGrow = style({})

// ── Spinner color variants (absorbing Bootstrap text-* utility classes) ───────

export const spinnerPrimary = style({})
export const spinnerSecondary = style({})
export const spinnerSuccess = style({})
export const spinnerDanger = style({})
export const spinnerWarning = style({})
export const spinnerInfo = style({})
export const spinnerLight = style({})
export const spinnerDark = style({})
