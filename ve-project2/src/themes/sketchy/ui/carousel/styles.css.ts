// Sketchy has no carousel-specific overrides — all Bootstrap defaults apply.
// This file exists solely as a side-effect import hook so that the theme
// stylesheet bundle is consistent across all families.
export {}
